<% if @post.errors.empty? %>
  <%= turbo_stream.prepend 'posts', partial: 'post', locals: { post: @post } %> 
<% else %>
  <%= turbo_stream.update 'new_post', partial: 'form', locals: { post: @post } %>
<% end %>

<%= render_turbo_stream_flash_messages %>