<%= form_with model: [comment.post, comment] do |form| %>
  <%= form.text_field :content, placeholder: 'Comment', size: '100%' %>
  <%= form.submit %>
  <%= link_to 'Close', comment.post, class: 'button' if request.headers['Turbo-Frame'] %>
<% end %>