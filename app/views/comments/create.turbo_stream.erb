<%= turbo_stream.append 'comments', @comment %>
<%= turbo_stream.update :new_comment, partial: "comments/form", locals: { comment: @post.comments.new } %>
<%= turbo_stream.append :comments, @comment %>
<%= render_turbo_stream_flash_messages %>
